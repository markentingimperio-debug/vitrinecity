# Daily prayer media and distribution

Owner request, 2026-09-11: one prayer daily at 07:00 America/Sao_Paulo, a 30-second vertical video and a TikTok version at least 60 seconds. Start 2026-09-12; the first edition contains real 30/61-second H.264/AAC files. Existing editorial calendar remains the source of prayers.

On 2026-09-12 the owner requested 65 seconds for new TikTok renders. Existing ready editions (including 61-second files already generated) and their hashes/receipts remain unchanged. The local Jarvis supervisor was updated for the requested 11h useful-content and 18h Lia-presenter slots, preparing at 10h/17h; these are not additional autonomous VPS video publishers. The Lia identity and provider connections still need completion before the presenter slot can publish.

`/admin-oracoes.html` shows selected destinations, prepared media, receipt counts, outstanding connections and pause/resume. `GET /api/admin/prayer-sharing` is admin-only. Public dated media is exposed through narrowly validated `/prayer-media/:day/:format.mp4`, with native browser range support; private narration/receipts are not served.

The SQL setting is disabled by default. Activation requires the authorized snapshot of named WhatsApp groups and the existing Instagram target `@agrotecniica` (account 7, Campo & Conhecimento). Preserve prior group exclusions, especially Receitas 06. Do not automatically enroll new groups. Each send rechecks live membership and posting permission, current settings and global pause. Delivery starts at 07:00, staggered through the existing 3-per-tick worker, and expires at 07:45. No late backfill. Stable date/group IDs, atomic claims and provider receipts prevent duplicate retries after uncertain submissions.

The generator prepares one edition ahead. Two narration/transcription jobs per day use the existing OpenAI configuration; durable intent files precede chargeable requests, and interrupted/uncertain jobs require review. FFmpeg renders 720×1280 portrait, 30 fps, H.264 yuv420p with AAC, subtitle timing follows the recorded voice, and output duration is checked. Existing ready media is immutable by default. `font-dejavu` is included in the image. AI art and voice are disclosed in videos/captions. This is motion composition of the existing artwork, not a claim of newly filmed footage.

Owner clarification, 2026-09-12: daily distribution includes Facebook Page Reels and Facebook/Instagram Stories, as well as the existing Instagram Reel and selected WhatsApp groups. The new `social_enabled` setting defaults to 1 for those three added destinations, including existing installations; the master `enabled` setting remains disabled by default and the global pause still applies. `instagram_enabled` and the original Instagram journal are unchanged. No new groups are enrolled by this migration.

The Meta adapter uses four separate dated journals and SQL states: `instagram`, `facebook`, `instagram-stories`, `facebook-stories`. Target is the existing verified Campo & Conhecimento Page / `@agrotecniica` binding. Each write is guarded by current channel settings, settings revision, start day, global pause and the 07:00–07:45 window. Receipt verification is channel-specific; one successful Instagram Reel does not mark Facebook or Stories as published. The admin panel shows independent channel names, publication states and verified links when the format provides them. Stories expire, so verified receipts are retained as historical evidence rather than republished.

Read-only receipt reconciliation continues after the publication window, on subsequent days and while paused; it never starts a new late publication. Recent journal filenames are also inspected to recover a persisted receipt missing its SQL summary after a crash. Transient GET/network/429/5xx failures retain the journal phase (or `retry_pending` before the first journal save) and can be checked again; they do not permanently hold a whole day. A persisted uncertain POST intent or `held_unknown` remains read-only and cannot trigger automatic resubmission. Integrity, permission or binding failures require review. Already verified publications are not sent or verified again, preserving expiring Stories and the original Instagram receipts.

YouTube and Vitrine Social use separately injected adapters with `status()`, `pendingDays()` and `publish({day,manifest,canPublish,mode})`. They obey the same master pause, start day and 07:00–07:45 publication window. Both are inactive without a configured/confirmed publisher. YouTube additionally requires OAuth for the exact intended channel; `/admin-youtube.html` manages that connection. Its independent `youtube_enabled` flag defaults to 0 even in existing installations: connecting does not activate posting. After the channel is confirmed, an administrator explicitly enables YouTube through its button in `/admin-oracoes.html`; disabling YouTube leaves the other destinations untouched. A private/unlisted or processing video is never reported as publicly published. Existing `prayer_channel_runs` records marked `published_verified` (including a verified native YouTube Studio publication) prevent duplicate automatic upload. After the window, only receipt reconciliation is allowed, including the existing resumable-session status probe without uploading new bytes. Uncertain, private and review states are read-only in the scheduler. Vitrine Social retains its own public post and lifecycle receipt; success in another network cannot mark it published.

TikTok authorization was renewed and creator info checked, but app configuration remains sandbox and the required publishing/visibility flow is outstanding; it is not an automatic destination here. Facebook group distribution is a manual/native scheduling package; there is no configured supported Groups publisher. These channels are explicitly shown as pending/manual, not active automatic sends. Facebook Page publishing does not imply that Facebook groups received the video.

The dedicated prayer group is permitted only for the `prayer-v1:` campaign, while the same group's commercial exclusion remains in effect. The shared worker checks exclusions per schedule, so the prayer exception cannot allow promotions. Receitas 06 remains excluded from automation.

Validation: `node --test scripts/test-prayer-daily.mjs scripts/test-prayer-page.mjs scripts/test-prayer-media.mjs scripts/test-prayer-sharing.mjs scripts/test-whatsapp-schedule-worker.mjs`. Tests must use an isolated SQLite database and fake network; never send test messages to real groups.

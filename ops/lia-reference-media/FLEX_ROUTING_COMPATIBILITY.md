# Reference compatibility for PR 213

The v96 flexible routing overlay depends on this v95 feature and requires `LIA reference media validation` on the exact head revision. This note retains the original path-filtered workflow in the PR diff. The original reference module, builder hashes, 46 tests and lifecycle suite remain unchanged; v96 is supplied separately under `ops/lia-flex-media`.

The new suite additionally exercises the user's misspelled product-reference request, ambiguous media choices and negative instructions before paid preparation. Generic image analysis, speech-language selection and TTS are not part of this routing overlay.
